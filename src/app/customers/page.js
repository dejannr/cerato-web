import { caseStudies } from "@/content/case-studies";
import { CaseStudyCard, FinalCta, PageHero } from "@/components/shared/site-components";
export const metadata = { title: "Customers — Cerato Systems", description: "See how Cerato Systems turns complex business workflows into custom operational software." };
export default function CustomersPage() { return <><PageHero eyebrow="Customers" title="Software built around real businesses." primary={false}><p>Every business has its own workflows, constraints, and operational complexity. Our work starts by understanding those details and building software around them.</p><p>The stories below show how custom systems can replace fragmented processes, improve control, and give teams better tools for running the business.</p></PageHero><section className="container page-section case-grid customers-case-grid">{caseStudies.map((study) => <CaseStudyCard key={study.slug} study={study} />)}</section><FinalCta title="Have an operation that has outgrown its tools?" body="We build custom software around workflows that generic systems cannot quite fit." /></>; }
