"use client";
import { useState } from "react";
import { trackEvent } from "@/lib/analytics";
import { ArrowUpRight } from "@untitledui/icons/ArrowUpRight";
const contexts = ["Internal operations", "Process automation", "Customer application", "Employee / field application", "Integrations", "Existing custom software", "Not sure yet"];
export function ContactForm() { const [status, setStatus] = useState("idle"); const [started, setStarted] = useState(false); async function submit(event) { event.preventDefault(); const form = event.currentTarget; if (!form.checkValidity()) { form.reportValidity(); return; } setStatus("sending"); try { const response = await fetch("/api/contact", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(Object.fromEntries(new FormData(form))) }); if (!response.ok) throw new Error("Delivery unavailable"); setStatus("success"); trackEvent("contact_form_submitted"); form.reset(); } catch { setStatus("error"); } } return <form className="contact-form" onSubmit={submit} onFocus={() => { if (!started) { setStarted(true); trackEvent("contact_form_started"); } }}><p className="form-note">PLACEHOLDER: CONTACT FORM DELIVERY</p><div className="form-grid"><label><span className="field-label">Your name <b aria-hidden="true">*</b></span><input name="name" placeholder="Jane Smith" required /></label><label><span className="field-label">Work email <b aria-hidden="true">*</b></span><input name="email" type="email" placeholder="jane@company.com" required /></label><label><span className="field-label">Company <b aria-hidden="true">*</b></span><input name="company" placeholder="Company name" required /></label><label><span className="field-label">Company website</span><input name="website" type="url" placeholder="company.com" /></label></div><fieldset><legend>Project context</legend><div className="context-options">{contexts.map((context) => <label key={context}><input type="checkbox" name="context" value={context} /><span>{context}</span></label>)}</div></fieldset><label><span className="field-label">What are you trying to improve? <b aria-hidden="true">*</b></span><textarea name="message" required placeholder="Tell us about the workflow, process, or operational problem you want to improve. What are you using today, and what is no longer working?" /></label>{status === "success" && <p className="form-success" role="status">Thanks — your message has been received. We&apos;ll get back to you as soon as we can.</p>}{status === "error" && <p className="form-error" role="alert">Something went wrong while sending your message. Please try again or contact us by email. <span>PLACEHOLDER: COMPANY EMAIL</span></p>}<button className="button-primary" type="submit" disabled={status === "sending"}>{status === "sending" ? "Sending…" : "Send project inquiry"} <ArrowUpRight /></button></form>; }
